@model Generic.Data.PhilHealthSource

<div class="container-fluid">
    <h1 class="h3 mb-1 text-gray-800">Manage PhilHealth Source</h1>

    <div class="d-flex justify-content-end mb-3">
        <button class="btn btn-sm btn-primary px-4" data-url="/PhilHealthSource/Create/" data-id="createPhilHealthSource" data-dialog-title="Create PhilHealth Source" data-dialog-width="modal-lg" data-process-button="Create" onclick="loadAndShowModal($(this))"><i class="fas fa-plus mr-2 ml-0"></i>Create</button>
    </div>

    <div class="table-responsive-md">
        <table id="PhilhealthDT" class="table table-hover table-striped table-bordered nowrap" style="width:100%">
            <thead>
                <tr class="text-center">
                    <th>Source</th>
                    <th>IsAcitve</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>


@section Scripts {
    <script src="@Url.Content("~/js/PhilHealthSource/Index.js")"></script>
}